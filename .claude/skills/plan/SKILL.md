---
name: plan
description: GitHub Issue を参照して実装プランを作成する。ローカルではファイル出力、CI 環境では Issue コメントとして投稿する。
argument-hint: "[issue-number]"
---

# Implementation Plan Skill

GitHub Issue を参照し、コードベースを調査した上で実装プランを作成する。

## 前提条件

- `gh` CLI が認証済みであること

## 手順

### 1. Issue の取得

#### Issue 番号の解決ルール

引数と実行環境に応じて、対象 Issue を以下の優先順で決定する:

| 条件 | 動作 | 例 |
|------|------|-----|
| 引数に Issue 番号が指定されている | 指定された Issue を取得する | `/plan 42` |
| 引数なし + `CI=true` | 現在の Issue を対象とする（`GITHUB_ISSUE_NUMBER` から取得） | `/plan` |
| 引数なし + ローカル | エラー（Issue 番号の指定が必要） | — |

```bash
# Issue 番号の決定
if [ -n "$ARGUMENTS" ]; then
  ISSUE_NUMBER="$ARGUMENTS"
elif [ "$CI" = "true" ]; then
  ISSUE_NUMBER=${GITHUB_ISSUE_NUMBER:-}
  if [ -z "$ISSUE_NUMBER" ]; then
    echo "Error: Issue number not found in CI context"
    exit 1
  fi
else
  echo "Error: Issue number is required"
  exit 1
fi
```

```bash
gh issue view ${ISSUE_NUMBER}
```

Issue のタイトル、本文、ラベル、コメントを取得し、要件を正確に理解する。

### 2. コードベースの調査

Issue の要件に基づいて、関連するコードを徹底的に調査する。

- **既存の実装パターン**: 同様の機能がどのように実装されているかを確認する
- **影響範囲**: 変更が影響するパッケージ（`apps/desktop`、`packages/shared`、`packages/ui`）を特定する
- **拡張ポイント**: IPC チャネル、ドメインマネージャー、React コンポーネント、リポジトリなど、活用すべき拡張ポイントを確認する
- **テスト構成**: 既存のテスト構成（Vitest ユニットテスト、Playwright E2E テスト、Storybook）を確認する

### 3. 実装プランの作成

以下の観点で実装プランを設計する:

- **実現可能性**: 要件が現在のアーキテクチャで実現可能かを検証する
- **設計判断**: 複数のアプローチがある場合は、各アプローチのトレードオフを分析する
- **段階的な実装**: 変更を論理的なステップに分割し、各ステップの依存関係を明確にする
- **テスト戦略**: 各変更に対するテスト方針を策定する

### 4. 実装プランの出力

実行環境に応じて出力先を切り替える。環境変数 `CI` の有無で判定すること。

#### ローカル実行時 (`CI` が未設定)

実装プランを `.claude/outputs/plans/` ディレクトリにファイルとして出力する。

- ディレクトリが存在しない場合は作成すること
- ファイル名: `PLAN-<Issue番号>-<Issueタイトルをケバブケースに変換>.md`
  - 例: Issue #42 "Add Threads support" → `PLAN-42-add-threads-support.md`
  - タイトルに含まれる特殊文字は除去し、スペースをハイフンに変換する

#### GitHub Actions 実行時 (`CI=true`)

作成した実装プランを GitHub Issue にコメントとして投稿する。

```bash
gh issue comment ${ISSUE_NUMBER} --body "$(cat <<'EOF'
<実装プランのMarkdown>
EOF
)"
```

## 実装プランのフォーマット

以下のフォーマットに従って Markdown を生成する。

````markdown
## 実装プラン

> 📋 Issue: #<issue-number>
> 📅 作成日: YYYY-MM-DD

### 概要

<!-- Issue の要件を簡潔に要約し、実装方針の全体像を述べる -->

### 設計判断

<!-- 複数のアプローチがある場合のみ記載。なければセクションごと省略する -->

| アプローチ | メリット | デメリット |
|-----------|---------|-----------|
| A: <方式名> | <メリット> | <デメリット> |
| B: <方式名> | <メリット> | <デメリット> |

**採用案**: <選択したアプローチとその理由>

### 影響範囲

| パッケージ | 影響 | 備考 |
|-----------|------|------|
| `apps/desktop` | 新規 / 変更 / なし | <概要> |
| `packages/shared` | 新規 / 変更 / なし | <概要> |
| `packages/ui` | 新規 / 変更 / なし | <概要> |

### ファイル構成

<!-- 変更・追加されるファイルの構成を示す。新規追加は (new)、変更は (modify) で表記する -->

```
apps/desktop/src/
├── main/
│   └── new-handler.ts          (new)
├── renderer/
│   └── hooks/useNewFeature.ts  (new)
packages/shared/src/
├── domain/
│   └── NewManager.ts           (new)
packages/ui/src/
├── features/
│   └── new-feature/
│       ├── NewFeature.tsx       (new)
│       └── NewFeature.module.css (new)
```

### 実装ステップ

<!-- 実装を Phase（論理的なまとまり）と Step（具体的な作業単位）に分割する -->

#### Phase 1: <フェーズのタイトル>

<!-- フェーズの目的・ゴールを簡潔に述べる -->

##### Step 1: <ステップのタイトル>

**対象**: `path/to/file.ts`

<!-- 具体的な変更内容を記述する。必要に応じてコード例を含める -->

```typescript
// コード例（必要な場合のみ）
```

##### Step 2: <ステップのタイトル>

<!-- 同様のフォーマットで記述 -->

#### Phase 2: <フェーズのタイトル>

##### Step 1: <ステップのタイトル>

<!-- 同様のフォーマットで記述 -->

### ドキュメント更新

<!-- 実装に伴い更新が必要なドキュメントを記載する -->

| ドキュメント | 更新内容 |
|-------------|---------|
| `README.md` | <更新内容の概要> |
| `CLAUDE.md` | <更新内容の概要> |

### テスト戦略

| テスト種別 | 対象 | テスト内容 |
|-----------|------|-----------|
| ユニットテスト (Vitest) | `packages/shared/src/domain/xxx` | <テスト内容> |
| E2E テスト (Playwright) | `apps/desktop` | <テスト内容> |
| Storybook | `packages/ui/src/features/xxx` | <テスト内容> |

### 確認事項

<!-- Issue のオーナーに確認すべき事項があれば記載する。なければセクションごと省略する -->

- [ ] <確認事項1>
- [ ] <確認事項2>

### 今後の展望

<!-- この Issue のスコープ外だが、関連する今後の拡張や改善の可能性を記載する。なければセクションごと省略する -->

- **<拡張テーマ>**: <概要と期待される効果>
- **<拡張テーマ>**: <概要と期待される効果>

---
🤖 *Generated by Claude Code*
````

## 注意事項

- Issue の要件を正確に理解し、過不足のないプランを作成すること
- 推測ではなく、実際にコードを読んで確認した事実に基づいてプランを作成すること
- 実装ステップは具体的に記述し、各ステップで変更するファイルパスを明示すること
- 既存のアーキテクチャパターンや命名規則に従った設計にすること
- モノレポの各パッケージの責務を理解し、適切なパッケージにコードを配置すること
  - `packages/shared`: フレームワーク非依存のビジネスロジック（Electron/React に依存しない）
  - `packages/ui`: 純粋な React コンポーネント（Electron API に依存しない）
  - `apps/desktop`: Electron 固有のコード（メインプロセス、IPC、プリロード）
- 不明点や判断が必要な事項は「確認事項」セクションに明記すること
- 変更のない「設計判断」「確認事項」「今後の展望」セクションは省略してよい
- 「今後の展望」は Issue のスコープ外の関連拡張を記載する。実装プラン策定時にコードベースを調査する中で見つけた改善機会や、自然な次のステップとなる機能拡張を挙げること
- 実装に伴うドキュメント（`README.md`、`CLAUDE.md`）の更新を必ず「ドキュメント更新」セクションに記載すること。新機能・設定変更・アーキテクチャ変更がある場合、対応するドキュメントの更新箇所を具体的に特定すること
